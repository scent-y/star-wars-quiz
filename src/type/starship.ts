export type Starship = {
    name: string | null,
    model: string | null,
    starship_class: string | null,
    manufacturer: string | null,
    crew: string | null,
    passengers: string | null,
    pilots: string[] | null
}
